const html = await (await fetch('https://www.lurvox.in/?t=' + Date.now())).text()
const themeBlock = html.match(/Shopify\.theme\s*=\s*(\{[\s\S]*?\});/)
console.log(themeBlock?.[1]?.slice(0, 500))
const cdn = [...html.matchAll(/cdn\.shopify\.com\/s\/files\/[^"']+\/t\/(\d+)\//g)].slice(0, 5)
console.log('cdn theme nums', cdn.map((m) => m[1]))
const shopT = [...html.matchAll(/\/cdn\/shop\/t\/(\d+)\//g)].slice(0, 5)
console.log('shop t', shopT.map((m) => m[1]))
console.log('has redirect in fab area', html.includes('lurvox-talk-path-redirect-v1'))
console.log('fab href talk-coach', html.includes('lurvox-fab__btn--primary"\n        href="/pages/talk-coach"') || html.includes('href="/pages/talk-coach"'))
