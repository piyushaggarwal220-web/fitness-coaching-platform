# Phase 20 — Experimentation

Extends `marketing_experiments` (no fork). Jarvis lifecycle + data sufficiency.

Single experiments do not auto-mint OPERATING_RULE (Phase 14 gates).

No live Meta/IG writes from experiment tools.
