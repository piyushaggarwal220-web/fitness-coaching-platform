/** Coach portal theme — light/white tokens without affecting client or admin. */

export {
  coachColors as colors,
  spacing,
  radius,
  layout,
  shadows,
  motion,
  transition,
  typography,
} from '@/lib/design-tokens'
